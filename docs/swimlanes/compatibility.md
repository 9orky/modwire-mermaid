# swimlane compatibility

This file is generated by `scripts/generate_compatibility.py`; do not edit it manually.
Upstream syntax: [https://mermaid.js.org/syntax/swimlanes.html](https://mermaid.js.org/syntax/swimlanes.html).
The fixtures are built with strict frozen Pydantic contracts and compiled through `ModwireMermaidFactory.standard()`.

## Feature inventory

| Feature | Status | Contract | Evidence |
| --- | --- | --- | --- |
| `lanes-cross-lane-edges` | supported | Emitted by the typed model and exercised by the corpus. | `swimlane.minimal`, `swimlane.comprehensive` |
| `flowchart-nodes-icons-images-markdown-styles-interactions` | supported | Emitted by the typed model and exercised by the corpus. | `swimlane.focused-flowchart-reuse` |
| `stability` | partial | Mermaid documents swimlane syntax as experimental/beta in 11.16.0. | — |

## Executable fixtures

### `swimlane.minimal`

Snapshot: [`swimlane.minimal.mmd`](../../compatibility/snapshots/source/swimlane.minimal.mmd).

```mermaid
swimlane-beta TB
  subgraph team ["Team"]
    task@{ shape: rect, label: "Task" }
  end
```

### `swimlane.comprehensive`

Snapshot: [`swimlane.comprehensive.mmd`](../../compatibility/snapshots/source/swimlane.comprehensive.mmd).

```mermaid
swimlane-beta TB
  accTitle: Ownership
  accDescr: Cross-lane handoff
  %% beta reuse proof
  subgraph customer ["Customer"]
    request@{ shape: circle, label: "`**Request**`" }
    person@{ icon: "fa:fa-user", label: "Person", pos: "b", h: 48 }
  end
  subgraph system ["System"]
    asset@{ img: "data:image/svg+xml;base64,PHN2ZyB4bWxucz0iaHR0cDovL3d3dy53My5vcmcvMjAwMC9zdmciLz4=", label: "Asset", pos: "b", w: 64, h: 64, constraint: "on" }
    decision@{ shape: diam, label: "Ready?" }
  end
  request s0@-->|"owns"| person
  person s1@-.-> asset
  asset s2@==> decision
  click request href "https://example.test/request" "Open request" _self
  style decision fill:#ffd
  linkStyle 1 stroke:blue;
  classDef primary color:#111;
  class request primary;
```

### `swimlane.focused-flowchart-reuse`

Snapshot: [`swimlane.focused-flowchart-reuse.mmd`](../../compatibility/snapshots/source/swimlane.focused-flowchart-reuse.mmd).

```mermaid
swimlane-beta TB
  accTitle: Ownership
  accDescr: Cross-lane handoff
  %% beta reuse proof
  subgraph customer ["Customer"]
    request@{ shape: circle, label: "`**Request**`" }
    person@{ icon: "fa:fa-user", label: "Person", pos: "b", h: 48 }
  end
  subgraph system ["System"]
    asset@{ img: "data:image/svg+xml;base64,PHN2ZyB4bWxucz0iaHR0cDovL3d3dy53My5vcmcvMjAwMC9zdmciLz4=", label: "Asset", pos: "b", w: 64, h: 64, constraint: "on" }
    decision@{ shape: diam, label: "Ready?" }
  end
  request s0@-->|"owns"| person
  person s1@-.-> asset
  asset s2@==> decision
  click request href "https://example.test/request" "Open request" _self
  style decision fill:#ffd
  linkStyle 1 stroke:blue;
  classDef primary color:#111;
  class request primary;
```

## Version boundary

Swimlane uses experimental `swimlane-beta` syntax and therefore requires Mermaid 11.16.0 or newer.
