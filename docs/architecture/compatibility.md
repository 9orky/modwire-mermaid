# architecture compatibility

This file is generated by `scripts/generate_compatibility.py`; do not edit it manually.
Upstream syntax: [https://mermaid.js.org/syntax/architecture.html](https://mermaid.js.org/syntax/architecture.html).
The fixtures are built with strict frozen Pydantic contracts and compiled through `ModwireMermaidFactory.standard()`.

## Feature inventory

| Feature | Status | Contract | Evidence |
| --- | --- | --- | --- |
| `services` | supported | Emitted by the typed model and exercised by the corpus. | `architecture.minimal`, `architecture.comprehensive` |
| `groups` | supported | Emitted by the typed model and exercised by the corpus. | `architecture.comprehensive` |
| `junctions` | supported | Emitted by the typed model and exercised by the corpus. | `architecture.comprehensive` |
| `edges` | supported | Emitted by the typed model and exercised by the corpus. | `architecture.comprehensive` |
| `service-alignment` | unsupported | Mermaid row/column alignment has no typed field. | — |
| `architecture-configuration` | unsupported | Architecture configuration is not modeled. | — |

## Executable fixtures

### `architecture.minimal`

Snapshot: [`architecture.minimal.mmd`](../../compatibility/snapshots/source/architecture.minimal.mmd).

```mermaid
architecture-beta
  service api[API]
```

### `architecture.comprehensive`

Snapshot: [`architecture.comprehensive.mmd`](../../compatibility/snapshots/source/architecture.comprehensive.mmd).

```mermaid
architecture-beta
  title Cloud topology
  group cloud(cloud)[Cloud]
  group apps[Applications] in cloud
  service web(server)[Web] in apps
  service db(database)[Database] in cloud
  junction gateway in cloud
  web:R --> gateway:L
  gateway:B <--> db:T
```
