# State diagrams

```python
from modwire_mermaid.state.diagram import ModwireStateDiagram
```

`ModwireStateDiagram` supports named states, start and end markers, labelled transitions, choices,
forks, joins, comments, and all Mermaid state-diagram directions.
