# Mindmaps

```python
from modwire_mermaid.mindmap import ModwireMindmap
```

`ModwireMindmap` supports typed hierarchy, every Mermaid mindmap shape, plain and Markdown text,
icons, CSS classes, Unicode, and default or tidy-tree layout.
